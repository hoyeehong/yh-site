# yh-site

Personal portfolio — plain HTML/CSS with a little vanilla JS, no build step required.

## Structure

```
public/   ← all site files (HTML, CSS, JS, assets)
```

## Deploy

Push to `main`. GitHub Actions automatically publishes `public/` to GitHub Pages.
